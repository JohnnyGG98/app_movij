import 'package:flutter/material.dart';

class AppThemeCons {
  static const PADDING_LIST_GAMES = EdgeInsets.only(
    top: 20,
    left: 20,
    right: 20,
  );

  static const PADDING_BUTTON_GAMES = EdgeInsets.symmetric(
    horizontal: 10,
  );
}
